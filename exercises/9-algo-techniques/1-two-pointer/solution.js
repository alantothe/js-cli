/**
 * Two Pointer Technique
 *
 * This technique uses two pointers moving at different speeds or directions
 * to solve problems efficiently without nested loops.
 */

// Implement a function that finds if a sorted array contains two numbers that sum to a target
function twoSum(arr, target) {
  // TODO: Implement two pointer approach
  // Start with one pointer at the beginning and one at the end
  // Move pointers toward each other based on the sum
}

module.exports = { twoSum };
