/**
 * Course Schedule Patterns
 */

function canFinish(numCourses, prerequisites) {
  // TODO: Return true if all courses can be completed
}

function findOrder(numCourses, prerequisites) {
  // TODO: Return one valid topological order, or [] if impossible
}

function minimumSemesters(n, relations) {
  // TODO: Return min semesters to complete courses labeled 1..n, or -1 if impossible
}

module.exports = { canFinish, findOrder, minimumSemesters };
