/**
 * ╔══════════════════════════════════════════╗
 * ║        Top K Problems with Heaps         ║
 * ╚══════════════════════════════════════════╝
 *
 * Common interview pattern: find top/bottom K elements efficiently.
 */

// Find the Kth largest element in an unsorted array
// You can sort or use a min-heap of size K
function findKthLargest(nums, k) {
  // TODO: Find the kth largest element
}

// Find the K most frequent elements in an array
function topKFrequent(nums, k) {
  // TODO: Count frequencies, then find top K
}

// Sort an array of points by distance to origin, return closest K
// points = [[x1,y1], [x2,y2], ...]
function kClosestPoints(points, k) {
  // TODO: Calculate distances and find K closest
}

module.exports = { findKthLargest, topKFrequent, kClosestPoints };
