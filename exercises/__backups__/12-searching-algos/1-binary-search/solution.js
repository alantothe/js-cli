/**
 * Binary Search Algorithm
 *
 * An efficient search algorithm for sorted arrays that divides the search space in half
 * with each iteration, achieving O(log n) time complexity.
 */

// Find the index of a target value in a sorted array
function binarySearch(arr, target) {
  // TODO: Implement binary search
  // Use left and right pointers to divide search space in half
  // Return index if found, -1 if not found
}

module.exports = { binarySearch };
