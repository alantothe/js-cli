/**
 * ╔══════════════════════════════════════════╗
 * ║          Insertion Sort                  ║
 * ╚══════════════════════════════════════════╝
 *
 * Simple sorting algorithm, efficient for small or nearly-sorted data.
 * Time: O(n²) worst, O(n) best | Space: O(1) | Stable: Yes
 */

// Sort array using insertion sort (in-place)
function insertionSort(arr) {
  // TODO: For each element, insert it into correct position in sorted portion
}

module.exports = { insertionSort };
