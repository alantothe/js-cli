/**
 * ╔══════════════════════════════════════════╗
 * ║     Two Pointer Pattern Problems         ║
 * ╚══════════════════════════════════════════╝
 *
 * Problems solved efficiently with the two pointer technique.
 */

// Valid Palindrome: Check if string is a palindrome (ignore non-alphanumeric, case-insensitive)
function isPalindrome(s) {
  // TODO: Use two pointers from both ends, skip non-alphanumeric
}

// Container With Most Water: Find two lines that form container holding most water
// height[i] is the height of line at position i
function maxArea(height) {
  // TODO: Start from both ends, move the shorter line inward
}

// Three Sum: Find all unique triplets that sum to zero
function threeSum(nums) {
  // TODO: Sort, fix one number, use two pointers for remaining
}

// Remove Duplicates from Sorted Array: Return new length after removing duplicates in-place
function removeDuplicates(nums) {
  // TODO: Use slow/fast pointer
}

module.exports = { isPalindrome, maxArea, threeSum, removeDuplicates };
