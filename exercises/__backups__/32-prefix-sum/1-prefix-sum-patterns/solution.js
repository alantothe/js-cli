/**
 * Prefix Sum Patterns
 */

function buildPrefixSum(nums) {
  // TODO: Return prefix sums where prefix[i] = sum(nums[0..i])
}

function rangeSum(prefix, left, right) {
  // TODO: Return sum of original array from left to right inclusive
}

function subarraySumEqualsK(nums, k) {
  // TODO: Return count of subarrays that sum to k
}

function applyRangeIncrements(length, updates) {
  // TODO: Apply [start, end, inc] updates using difference array and return final array
}

module.exports = { buildPrefixSum, rangeSum, subarraySumEqualsK, applyRangeIncrements };
