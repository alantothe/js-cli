/**
 * LeetCode Array Problems
 */

/**
 * Problem: Next Permutation
 * Modify array in-place to the next lexicographically greater permutation
 *
 * @param {number[]} nums
 * @return {void} Modify array in-place
 */
function nextPermutation(nums) {
  // TODO: Implement next permutation
  // Hint: Find rightmost ascending pair, then rotate
}

/**
 * Problem: Rotate Array
 * Rotate array to the right by k steps in-place
 *
 * @param {number[]} nums
 * @param {number} k
 * @return {void} Modify array in-place
 */
function rotate(nums, k) {
  // TODO: Implement rotate array
  // Hint: Reverse operations can achieve rotation
}

/**
 * Problem: Product of Array Except Self
 * Return array where each element is product of all other elements
 * Constraint: Cannot use division
 *
 * @param {number[]} nums
 * @return {number[]}
 */
function productExceptSelf(nums) {
  // TODO: Implement product except self
  // Hint: Use left and right product arrays
}

module.exports = { nextPermutation, rotate, productExceptSelf };
