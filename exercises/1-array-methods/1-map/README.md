# Array Method: map()

## Overview
The `map()` method creates a new array populated with the results of calling a provided function on every element in the calling array.

## Key Concepts
- Returns a new array of the same length as the original
- The original array is **not mutated**
- Each element is **transformed** by the callback function

## Exercises
This exercise file contains 5 different problems to practice the `map()` method:

1. **Double each number** - Transform numbers by doubling them
2. **Extract names from objects** - Extract a specific property from objects in an array
3. **Convert strings to uppercase** - Transform strings to uppercase
4. **Generate HTML list items** - Create HTML strings for each item
5. **Add tax to products** - Transform objects by adding a new computed property

## Run Tests
```bash
npm test -- "solution.test.js"
```

## Difficulty
**Easy**
