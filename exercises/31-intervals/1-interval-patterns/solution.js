/**
 * Interval Patterns
 */

function mergeIntervals(intervals) {
  // TODO: Merge all overlapping intervals and return sorted merged output
}

function insertInterval(intervals, newInterval) {
  // TODO: Insert and merge newInterval into sorted non-overlapping intervals
}

function canAttendMeetings(intervals) {
  // TODO: Return false if any meetings overlap, otherwise true
}

function minMeetingRooms(intervals) {
  // TODO: Return minimum number of rooms required for all meetings
}

module.exports = { mergeIntervals, insertInterval, canAttendMeetings, minMeetingRooms };
