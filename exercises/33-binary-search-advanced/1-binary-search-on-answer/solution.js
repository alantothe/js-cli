/**
 * Binary Search on Answer
 */

function minEatingSpeed(piles, h) {
  // TODO: Return minimum k such that Koko can finish all piles within h hours
}

function shipWithinDays(weights, days) {
  // TODO: Return minimum ship capacity to deliver all packages in given days
}

function splitArrayLargestSum(nums, k) {
  // TODO: Split nums into k subarrays minimizing the largest subarray sum
}

module.exports = { minEatingSpeed, shipWithinDays, splitArrayLargestSum };
