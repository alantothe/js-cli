/**
 * Knapsack, LIS, String DP
 */

function canPartition(nums) {
  // TODO: Return true if nums can be partitioned into two equal-sum subsets
}

function lengthOfLIS(nums) {
  // TODO: Return length of longest increasing subsequence
}

function minDistance(word1, word2) {
  // TODO: Return edit distance between two strings
}

function coinChange(coins, amount) {
  // TODO: Return minimum coins needed, or -1 if impossible
}

module.exports = { canPartition, lengthOfLIS, minDistance, coinChange };
