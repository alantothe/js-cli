/**
 * Type Checker Helper Functions
 *
 * Implement the following type validation utilities
 */

function isArray(val) {
  // TODO: Check if value is an array
}

function isObject(val) {
  // TODO: Check if value is a plain object (not null, array, or function)
}

function isEmpty(val) {
  // TODO: Check if value is empty (empty string, array, or object)
}

function isNull(val) {
  // TODO: Check if value is null or undefined
}

module.exports = { isArray, isObject, isEmpty, isNull };
