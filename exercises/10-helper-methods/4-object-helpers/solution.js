/**
 * Object Helper Functions
 *
 * Implement the following object manipulation utilities
 */

function getKeys(obj) {
  // TODO: Return array of object keys
}

function getValues(obj) {
  // TODO: Return array of object values
}

function merge(obj1, obj2) {
  // TODO: Merge two objects (obj2 properties override obj1)
}

function isEmpty(obj) {
  // TODO: Check if object has no properties
}

module.exports = { getKeys, getValues, merge, isEmpty };
