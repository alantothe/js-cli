/**
 * Dijkstra & BFS Paths
 */

function shortestPathUnweighted(n, edges, src, dst) {
  // TODO: Return shortest distance from src to dst in unweighted graph, or -1
}

function dijkstra(n, edges, src) {
  // TODO: Return distance array from src in weighted graph edges [u,v,w]
}

function networkDelayTime(times, n, k) {
  // TODO: Return time for all nodes to receive signal, or -1 if impossible
}

module.exports = { shortestPathUnweighted, dijkstra, networkDelayTime };
